.article figure {
  margin-top: 1em;
  margin-bottom: 1.5em;
  font-size: .85em;
}

.article figure img {
  margin: 0;
}

.article figcaption {
  max-width: var(--readable-text-width);
  margin-top: .5em;
}
