import { styled, css } from 'astroturf';

export const Container = styled('header')`
  display: flex;
  align-items: center;
  flex-wrap: wrap;
`;

export const styles = css`
  .title {
    margin: 0;
    font-weight: 700;
    font-size: 1.5em;
  }
`;
